import 'package:flutter/material.dart';

class BulkAttendanceScreen extends StatelessWidget {
  const BulkAttendanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Select session and mark students present/absent/late'),
    );
  }
}
